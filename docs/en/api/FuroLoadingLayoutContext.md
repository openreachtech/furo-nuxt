# FuroLoadingLayoutContext

Context of `FuroLoadingLayout`. It exposes the loading flag to the template.

Extends [BaseFuroContext](./BaseFuroContext.md), so `.create()` takes `{ props, componentContext }`.

## Members

| member | description |
| :-- | :-- |
| `#get:isLoading` | The `isLoading` prop. |
